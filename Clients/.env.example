# API origin. Leave empty in production (nginx proxies /api and /uploads on the same origin).
# In development the Vite dev server proxies /api to http://127.0.0.1:8020, so this can stay empty too.
VITE_API_URL=
